# Supabase connection string — found in Project Settings > Database > Connection string
DATABASE_URL=postgres://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres

# Port the server listens on (default: 8080)
PORT=8080

# Log level
RUST_LOG=expense_tracker_backend=debug,tower_http=debug
